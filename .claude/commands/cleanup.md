Clean up Claude Code temporary files (tmpclaude-*, nul) from the working directory.

Run this command to remove leftover temp files:
```bash
rm -f tmpclaude-* nul 2>/dev/null
```

Report how many files were deleted.
